the database is sqlite
the Laravel 11 API app is in the backend folder
  to migrate run on vscode terminal: php artisan serve
  to run backend run on vscode terminal: php artisan serve

the Vue 3 Web app is in the fronten folder
  npm install
  npm run dev

  notes: make sure that the API port is 8000, e.g. http://127.0.0.1:8000